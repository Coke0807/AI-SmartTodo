root = "."
tmp_dir = "bin"

[build]
cmd = "go build -o ./bin/server ./cmd/server"
full_bin = "bin/server"
include_ext = ["go"]
exclude_dir = ["vendor", "bin", "tmp"]
exclude_regex = ["_test.go"]
delay = 1000

[log]
time = false

[misc]
clean_on_exit = true